package com.piprapay.companion.ui.theme

import androidx.compose.ui.graphics.Color

// Primary - Exact from APK colors.xml: main_color = #FF5947FD
val Primary = Color(0xFF5947FD)
val PrimaryLight = Color(0xFF8E59FF)
val PrimaryDark = Color(0xFF4A3AD4)

// Background - Exact from APK
val White = Color(0xFFFFFFFF)
val Background = Color(0xFFF9F9F9) // appBgMA
val CardBackground = Color(0xFFFFFFFF) // white_radius cards
val SurfaceVariant = Color(0xFFF6F7FA) // greyF6Ma

// Text - Exact from APK
val TextPrimary = Color(0xFF1D1E25) // blackMa
val TextSecondary = Color(0xFF808D9E) // greyMa
val TextTertiary = Color(0xFF7E8CA0) // grey7EMa
val TextHint = Color(0xFF9CA3AF)

// Status - Exact from APK
val Success = Color(0xFF60D39C) // greenMa
val SuccessBg = Color(0xFFDCFCE7)
val Error = Color(0xFFFF5555) // redMa
val ErrorBg = Color(0xFFFEE2E2)
val Warning = Color(0xFFFFC24B) // yellowMa

// Info - Exact from APK
val Info = Color(0xFF2280FF) // blueMa
val InfoLight = Color(0xFFCABDFF) // lightBlueMa

// Chart Colors - Exact from APK analytics layout
val ChartStored = Color(0xFF8E59FF) // Stored legend purple
val ChartUsed = Color(0xFF20BFF7) // Used legend cyan
val ChartError = Color(0xFFFF5555) // Error legend red

// Toggle - Exact from APK
val ToggleOn = Color(0xFF5947FD) // main_color
val ToggleOff = Color(0xFFD1D5DB)

// Divider - Exact from APK
val Divider = Color(0xFFD5D8DF) // greyDEMa
val DividerLight = Color(0xFFF3F3F3) // toolbar divider

// Bottom Nav - Exact from APK
val NavInactive = Color(0xFFA6ABB7) // button_unselect

// Input field border - Exact from APK
val InputBorder = Color(0xFFCDD0D7) // greyCDMa

// Button disabled - from APK
val ButtonDisabled = Color(0xFFA6ABB7) // button_shape_disible
